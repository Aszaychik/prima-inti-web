---
import AuthLayout from '~/layouts/AuthLayout.astro';
import Section from '~/components/Section.astro';
import Container from '~/components/Container.astro';
import Search from '~/components/admin/Search.astro';
---

<AuthLayout pageTitle="Manage Categories">
  <Section>
    <Container>
      <div class="bg-white rounded shadow p-6">
        <div class="flex justify-between items-center mb-6">
          <h1 class="text-2xl font-bold">Categories</h1>
          <div class="flex items-center gap-3">
            <Search placeholder="Search categories..." id="admin-search" />

            <button
              id="create-category-btn"
              class="bg-green-600 text-white px-4 py-1 rounded hover:bg-green-700"
              >+</button
            >
          </div>
        </div>

        <!-- Table container -->
        <div id="categories-table" class="overflow-x-auto">
          <table class="min-w-full border">
            <thead>
              <tr>
                <th class="border px-4 py-2">ID</th>
                <th class="border px-4 py-2">Name</th>
                <th class="border px-4 py-2">Description</th>
                <th class="border px-4 py-2">Created At</th>
                <th class="border px-4 py-2">Actions</th>
              </tr>
            </thead>
            <tbody id="categories-tbody">
              <tr><td colspan="5" class="text-center py-4">Loading...</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </Container>

    <!-- Modal for create/edit -->
    <div
      id="category-modal"
      class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50"
    >
      <div class="bg-white rounded-lg p-6 w-full max-w-md">
        <h2 id="modal-title" class="text-xl font-bold mb-4">Add Category</h2>
        <form id="category-form">
          <input type="hidden" id="category-id" />
          <div class="mb-4">
            <label class="block mb-1">Name</label>
            <input
              type="text"
              id="category-name"
              required
              class="w-full border px-3 py-2 rounded"
            />
          </div>
          <div class="mb-4">
            <label class="block mb-1">Description</label>
            <textarea
              id="category-description"
              rows="3"
              class="w-full border px-3 py-2 rounded"></textarea>
          </div>
          <div class="flex justify-end gap-2">
            <button
              type="button"
              id="modal-close"
              class="px-4 py-2 border rounded">Cancel</button
            >
            <button
              type="submit"
              class="bg-blue-600 text-white px-4 py-2 rounded">Save</button
            >
          </div>
        </form>
      </div>
    </div>

    <script>
      import { createAdminTable } from '~/services/adminTable';

      createAdminTable({
        endpoint: 'categories',
        tbodyId: 'categories-tbody',
        modalId: 'category-modal',
        formId: 'category-form',
        createBtnId: 'create-category-btn',
        closeBtnId: 'modal-close',
        modalTitleId: 'modal-title',
        idField: 'category-id',
        columns: [
          { key: 'id' },
          { key: 'name', escape: true },
          { key: 'description', escape: true },
          {
            key: 'created_at',
            render: (val) => new Date(val).toLocaleDateString(),
          },
        ],
        fields: [
          { id: 'category-name', key: 'name', required: true },
          { id: 'category-description', key: 'description' },
        ],
      });
    </script>
  </Section>
</AuthLayout>
